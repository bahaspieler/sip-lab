<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Unified UAC scenario for all proxy scenarios (02-04).

  Works with and without Record-Route:
  - rrs="true" on the 200 OK recv stores Record-Route headers if present
    (stores nothing if proxies don't add Record-Route — backward compatible)
  - [routes] in ACK/BYE expands to Route headers when Record-Route was stored,
    or expands to nothing when it wasn't
  - [next_url] uses the Contact from 200 OK as the Request-URI for ACK/BYE

  Also tolerates late provisional responses (180 arriving after 200 OK)
  which can happen with stateless proxies that forward independently.

  Flow:
    UAC sends INVITE   ->
    UAC recvs 1xx      <- (optional, any number of provisionals)
    UAC recvs 200 OK   <- (with Record-Route if proxies add it)
    UAC sends ACK      -> (with Route headers if Record-Route was present)
    (pause for call duration)
    UAC sends BYE      -> (with Route headers if Record-Route was present)
    UAC recvs 200 OK   <-
-->
<scenario name="UAC for proxy scenarios">

  <!-- Send INVITE with SDP offer -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "UAC" <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: "UAS" <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:uac@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=uac 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <!-- Absorb any provisional responses (100, 180, 183) -->
  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <!-- 200 OK: rrs="true" stores Record-Route headers (if any) for [routes] -->
  <recv response="200" rrs="true" rtd="true" crlf="true"/>

  <!-- ACK with Route headers derived from Record-Route (empty if no RR) -->
  <send>
    <![CDATA[
      ACK [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "UAC" <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: "UAS" <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      [routes]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Call duration pause. Duration set by -d flag on command line.
       NOTE: With a stateless proxy (scenario 02), a late 180 Ringing may
       arrive here after the 200 OK, causing SIPp to abort the call. This is
       a known SIPp limitation; the pcap capture is still correct. -->
  <pause/>

  <!-- BYE with Route headers (same route set as ACK; empty if no RR) -->
  <send retrans="500">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "UAC" <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: "UAS" <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      [routes]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" crlf="true"/>

</scenario>
