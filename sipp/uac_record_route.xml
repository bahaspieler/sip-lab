<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  UAC scenario with Record-Route support (scenario 05).

  Flow:
    UAC sends INVITE  → (via proxy chain)
    UAC recvs 100     ← (optional, from proxies)
    UAC recvs 180     ←
    UAC recvs 200 OK  ← (with Record-Route headers — stored via rrs="true")
    UAC sends ACK     → (with Route headers from Record-Route)
    (pause)
    UAC sends BYE     → (with Route headers from Record-Route)
    UAC recvs 200 OK  ←

  The rrs="true" attribute on the 200 OK recv stores the Record-Route
  headers. The [routes] keyword in subsequent sends expands them into
  Route headers, so ACK and BYE traverse the same proxy path as INVITE.
-->
<scenario name="UAC with Record-Route">

  <!-- Send INVITE with SDP offer -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "UAC" <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: "UAS" <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:uac@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=uac 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [auto_media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <!-- Absorb provisional responses -->
  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>

  <!-- 200 OK: store Record-Route headers for building Route set -->
  <recv response="200" rrs="true" rtd="true" crlf="true"/>

  <!-- ACK with Route headers derived from Record-Route -->
  <send>
    <![CDATA[
      ACK [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "UAC" <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: "UAS" <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      [routes]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Call duration pause. Set via -d flag on command line. -->
  <pause/>

  <!-- BYE with Route headers (same route set as ACK) -->
  <send retrans="500">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "UAC" <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: "UAS" <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      [routes]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" crlf="true"/>

</scenario>
